 /*
1.Объявить массив данных типа double размером 3 на 3. 
2.Ввести с консоли его значения. 
3.Вывести на консоль сумму его элементов, расположенных на главной диагонали. 
4.Вывести сумму элементов, расположенных на побочной диагонали. 
5.Объявить массив данных типа int размером 2 на 2. 
6.Ввести с консоли его значения. 
7.Вывести на консоль квадрат данной матрицы. 
/*

#include <stdio.h>

1.Объявить массив данных типа double размером 3 на 3. 
int main () 
    double m [3][3];
    for (i=0;i<3;i++);
    for (j=0;j<3;j++);
printf("%lf",m[i][j]);

2.Ввести с консоли его значения. 
printf("Введите значения для матрицы третьегопорядка: ");
for (i=0;i<3;i++);
     for(j=0;j<3;i++);
scanf("%lf", &massive[i][i]);
for (i=0;i<3;i++)

3.Вывести на консоль сумму его элементов, расположенных на главной диагонали. 
for (i=0;i<3;i++);
main += m[i][i]; 
printf ("Сумма элементов главной диагонали: %lf\n", main); 

4.Вывести сумму элементов, расположенных на побочной диагонали. 
not_main +=m[i] [2-i];
printf ("Сумма элементов побочной диагонали: %lf\n", not_main); 

5.Объявить массив данных типа int размером 2 на 2.
double matrix [2][2]; 
for for (i=0;i<2;i++);
    for (j=0;j<2;j++); 

6.Ввести с консоли его значения. 
printf("Введите значения для матрицы второго порядка: \n"); 
scanf("%d", &matrix[i] [j]); 

7.Вывести на консоль квадрат данной матрицы. 
for (i=0; i<2; i++) 
     for (j=0;j<2;j++); 
printf("%d", &matrix[i][j] * matrix[i][j]\n); 
return 0; 
