Ввести с консоли строку символов произвольной длины (до 80 символов). 
Поменять в ней все маленькие символы ‘a’ и ‘b’ на заглавные и 
вывести на консоль полученную строку.

#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main() {
    char input[81]; 
    printf("Введите строку: ");
    fgets(input, sizeof(input), stdin); 

    for (int i = 0; i < strlen(input); i++) {
        if (input[i] == 'a' || input[i] == 'b') {
            input[i] = toupper(input[i]); 
        }
    }
    printf("Результат: %s", input); 

    return 0;
}
